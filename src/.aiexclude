# Exclude dependency directories
node_modules/

# Exclude build and output directories
.next/
dist/
build/
public/

# Exclude configuration and lock files
package-lock.json
yarn.lock
.env

# Exclude sensitive or temporary files
apikeys.txt
secrets.json
.DS_Store

# Exclude version control metadata
.git/
.gitignore

# Exclude the specific large file identified in the error
/src/lib/learning-hub-articles.ts