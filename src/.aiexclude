# Exclude dependency and build directories
node_modules/
dist/
build/
.next/

# Exclude configuration and lock files
package-lock.json
yarn.lock
.env

# Exclude sensitive or temporary files
apikeys.txt
secrets.json
.DS_Store

# Exclude version control metadata (already ignored by .gitignore but good practice to include here)
.git/
